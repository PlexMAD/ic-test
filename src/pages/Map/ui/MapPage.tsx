import { Button } from "@/shared/ui/button";

export const MapPage = () => {
  return (
    <div>
      <div>Привет</div>
      <Button onClick={() => console.log("Привет")}>Привет</Button>
    </div>
  );
};
